# いつでも開けるようにする方法

スマホからPCなしでいつでも開くには、このフォルダのファイルをHTTPSで公開できるサービスに置きます。

## 置くファイル

- `index.html`
- `manifest.webmanifest`
- `sw.js`
- `icon.svg`

## いちばん簡単な方法

Netlify Dropに上の4ファイルをまとめてドラッグ＆ドロップすると、公開URLが発行されます。

https://app.netlify.com/drop

公開後、スマホでそのURLを開いて「ホーム画面に追加」すると、アプリのように起動できます。

## 注意

進捗データはブラウザ内に保存されます。別のスマホや別のブラウザとは自動同期されません。

## データベース連携

Supabaseでプロジェクトを作り、SQL Editorで `supabase-setup.sql` の内容を実行してください。

その後、サイト上部の「データベース連携」に以下を入力します。

- Supabase URL
- Supabase anon key

「DB接続」を押すと、以後は全端末で同じ進捗データを読み書きできます。
